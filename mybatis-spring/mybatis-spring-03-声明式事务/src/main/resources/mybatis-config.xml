<?xml version="1.0" encoding="UTF8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!-- 核心配置文件 -->
<configuration>

    <!-- 别名 -->
    <typeAliases>
        <package name="love.tanyiqu.pojo"/>
    </typeAliases>

    <!-- 映射配置文件 -->
    <mappers>
        <package name="love.tanyiqu.mapper"/>
    </mappers>

</configuration>